# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — never commit real credentials
.env
.env.local
.env.*.local
.env.development
.env.production
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo

# supabase
/supabase/.temp
/supabase/.branches

# Old WordPress site export. The inventory and report are tracked (small, and
# useful as a source list); the downloaded originals are not — 54MB of binaries
# that can be re-fetched with `npm run wp:export -- --download`.
/wordpress-export/images/
